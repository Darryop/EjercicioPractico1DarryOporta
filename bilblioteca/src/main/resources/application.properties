spring.application.name=Biblioteca

# Configuración del puerto
server.port=78

# Configuración de MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/biblioteca?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=DarryOP
spring.datasource.password=70741

# Configuración JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.cache=false

# Desarrollo
spring.devtools.restart.enabled=true
logging.level.com.biblioteca=DEBUG